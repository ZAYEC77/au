<%= render "_title.html", Map.put(assigns, :page_title, "Edit product") %>

<%= render "form.html", changeset: @changeset,
                        action: product_path(@conn, :update, @product) %>

<%= link "Back", to: product_path(@conn, :index) %>
